import React from 'react';
import Header from './Header';

const AddBook = (props) => {
    return (
        <div>
            <Header />
            <h1>
                Add New Book
            </h1>
        </div>
    );
};

export default AddBook;